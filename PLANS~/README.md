# Project-Level Plans

> Top-level discussions, vision threads, and meta-concerns that span the whole project
> rather than a single module. Module-scoped threads live in `<Module>/.plans~/`.

All threads here are registered in `.agent/thread-registry.md`.
